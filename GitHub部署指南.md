# Max的学习打卡台 - GitHub Pages 部署指南

## 概述
本指南将帮助你将"Max的学习打卡台"部署到GitHub Pages，实现公网访问，平板和手机都可以通过网址打开使用。

**GitHub Pages优势**：
- 完全免费
- 不需要实名认证（只需GitHub账号）
- 部署简单，几分钟即可完成
- 国内访问速度良好（平板打开一般3-5秒）

---

## 第一步：创建新仓库（约2分钟）

1. **登录GitHub**：在浏览器中打开 https://github.com 并登录你的账号
2. **新建仓库**：点击页面右上角的 **"+"** 号 → 选择 **"New repository"**（新建仓库）
3. **填写仓库信息**：
   - **Repository name**（仓库名称）：`max-study-desk`（建议用这个，简单好记）
   - **Description**（描述）：`Max的学习打卡台 - 猫小九历险记主题学习应用`（可选）
   - **Public / Private**：选择 **Public**（公开，GitHub Pages免费版必须公开）
   - **Initialize this repository with**：**不要勾选**任何选项（不要勾选README、.gitignore、license）
4. **创建仓库**：点击页面底部的绿色按钮 **"Create repository"**

---

## 第二步：上传应用文件（约3-5分钟）

### 方法一：网页拖拽上传（推荐新手使用）

1. **进入上传页面**：创建仓库后，你会看到一个空仓库页面，点击页面中的 **"uploading an existing file"** 链接（在"Quick setup"下方）
2. **准备文件**：在电脑上打开文件夹：
   ```
   D:\新建文件夹\在做\0. Workspace - 2026之后\4. 个人\7. 其他\育儿\20260905 学习工作台搭建\Max学习打卡台
   ```
3. **全选文件**：在文件夹中按 `Ctrl+A` 全选所有文件和文件夹（包括 `index.html`、`app.js`、`assets` 文件夹、`manifest.json`、`sw.js` 等）
4. **拖拽上传**：将选中的所有文件拖到GitHub网页的上传区域（虚线框内）
5. **等待上传**：等待所有文件上传完成（图片较多，可能需要1-3分钟，请耐心等待）
6. **提交文件**：上传完成后，滚动到页面底部，点击绿色按钮 **"Commit changes"**（提交更改）

### 上传完成后，仓库中应该包含以下文件：
```
max-study-desk/
├── index.html              （主页面，必须在根目录）
├── app.js                  （应用逻辑）
├── manifest.json           （PWA配置）
├── sw.js                   （Service Worker）
├── 使用说明.md
├── Gitee部署指南.md
├── GitHub部署指南.md
└── assets/                 （资源文件夹）
    ├── icon-192.png
    ├── icon-512.png
    ├── backgrounds/
    ├── characters/
    └── other_characters/
```

**重要**：`index.html` 必须在仓库的**根目录**，不能在子文件夹中！

---

## 第三步：开启GitHub Pages（约2分钟）

1. **进入仓库设置**：在仓库页面，点击顶部导航栏的 **"Settings"**（设置）
2. **找到Pages设置**：在左侧菜单中，向下滚动找到 **"Pages"**（在"Code and automation"分类下），点击进入
3. **配置部署源**：
   - **Source**（源）：选择 **"Deploy from a branch"**（从分支部署）
   - **Branch**（分支）：选择 **"main"**（或 `master`，根据你的仓库默认分支而定）
   - 文件夹选择：**"/ (root)"**（根目录）
4. **保存设置**：点击 **"Save"** 按钮
5. **等待部署**：页面顶部会显示 "Your site is ready to be published" 或 "Your site is live"
   - 首次部署可能需要1-2分钟
   - 可以刷新页面查看状态
6. **获取访问网址**：部署完成后，页面顶部会显示你的访问网址，格式为：
   ```
   https://你的用户名.github.io/max-study-desk/
   ```
   例如：如果你的GitHub用户名是 `zhangsan`，网址就是：
   ```
   https://zhangsan.github.io/max-study-desk/
   ```

---

## 第四步：平板上访问和使用（约2分钟）

### 方式一：浏览器直接访问
1. 在平板浏览器中输入上面的网址
2. 即可正常使用所有功能

### 方式二：添加到主屏幕（推荐，像App一样使用）
1. 在平板浏览器中打开网址
2. **华为平板浏览器**：点击浏览器底部菜单（四个点或三个点）→ "添加至" → "桌面"
3. **Chrome浏览器**：点击右上角三个点 → "添加到主屏幕"（Add to Home screen）
4. 确认添加后，平板桌面会出现"Max打卡台"图标
5. 以后点击桌面图标即可全屏打开应用，和原生App体验一致

---

## 重要说明

### 1. 数据存储
- 所有打卡数据、星星、修为等都存储在**浏览器本地**（localStorage）
- **不同设备间数据不共享**：平板上的数据只在平板上，手机上的数据只在手机上
- 清除浏览器缓存会丢失数据，建议定期在家长模式中导出数据备份

### 2. 更新应用
- 如果你修改了本地的 `index.html` 或 `app.js`，需要重新上传到GitHub仓库
- 上传方法：进入仓库页面 → 点击 **"Add file"** → **"Upload files"** → 拖拽修改后的文件 → 点击 "Commit changes"
- GitHub Pages会**自动重新部署**，一般1分钟内生效
- 平板上需要强制刷新（清除缓存）才能看到最新版本

### 3. 隐私安全
- 仓库是公开的，任何人都可以看到你的代码和图片资源
- 但**打卡数据只存储在用户的浏览器中**，不会上传到GitHub，所以孩子的学习数据是安全的
- 如果你不希望代码公开，可以升级GitHub Pro（付费）使用私有仓库的Pages服务

### 4. 访问速度
- GitHub Pages在国内访问速度良好，平板上打开一般在3-5秒内
- 首次加载会下载所有图片资源，可能稍慢；之后浏览器会缓存，打开速度很快
- 如果某些地区访问较慢，可以考虑使用Cloudflare CDN加速（免费，后续可配置）

---

## 常见问题

### Q: 部署后打开网址显示404怎么办？
A: 检查以下几点：
1. 确认 `index.html` 文件在仓库**根目录**（不是在子文件夹中）
2. 确认GitHub Pages的Branch选择正确（`main` 或 `master`）
3. 确认文件夹选择为 `/ (root)`
4. 等待1-2分钟，GitHub Pages部署需要时间
5. 强制刷新浏览器（Ctrl+F5 或 清除缓存）

### Q: 平板上打开后图片不显示怎么办？
A: 这通常是缓存问题：
1. 在平板浏览器中强制刷新（长按刷新按钮 → "强制刷新"）
2. 或者清除浏览器缓存后重新打开
3. 确认 `assets` 文件夹中的所有图片都已成功上传（在GitHub仓库页面检查文件列表）

### Q: 如何修改家长密码？
A: 在应用中点击"家长入口"，输入初始密码 `1234`，进入家长模式后可以在设置中修改密码。

### Q: 数据会丢失吗？
A: 只要不清除浏览器数据，数据会一直保存在平板上。建议：
1. 定期在家长模式中使用"导出数据"功能备份
2. 不要在平板的"设置"中清除浏览器数据
3. 不要使用"无痕模式"打开应用（无痕模式关闭后数据会丢失）

### Q: GitHub Pages和Gitee Pages有什么区别？
A:
| 对比项 | GitHub Pages | Gitee Pages |
|---|---|---|
| 实名认证 | 不需要 | 需要 |
| 国内访问速度 | 良好 | 很快 |
| 部署难度 | 简单 | 简单 |
| 费用 | 免费 | 免费 |

如果你已经有GitHub账号，推荐使用GitHub Pages，省去实名认证的步骤。

---

## 技术支持
如果在部署过程中遇到问题，可以：
1. 重新阅读本指南的对应步骤
2. 检查每一步的设置是否正确
3. 确认所有文件都已正确上传

祝使用愉快！
